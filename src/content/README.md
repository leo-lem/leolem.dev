# Content is synced at build time

`src/content/` is populated automatically during development and CI by copying from a separate private content repository.

Do not commit articles or other content to this folder.