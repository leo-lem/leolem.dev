---
import BaseLayout from "./BaseLayout.astro";

const {
  frontmatter: { title, description, subscribe }
} = Astro.props;
---

<BaseLayout title={title} description={description} subscribe={subscribe}>
  <article class="prose prose-invert card p-2 mx-auto">
    <slot />
  </article>
</BaseLayout>
