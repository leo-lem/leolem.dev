# Assets are synced at build time

`src/assets/` is populated automatically from the private content repository (and/or local fixtures). It is intentionally empty in git.

Do not commit images here. Use the content repo instead.